#pragma once
class Spawner
{
	// each requires a timer (for spawn rate)
	// type of enemy to spawn
	// spawn rate for each enemy type
	// how many to spawn at once per enemy type

	// function to get random position to spawn enemy
};
